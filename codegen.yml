overwrite: true
schema:
  - "src/GraphQL/schema.graphql"
  - |
    scalar Upload
documents: null
generates:
  src/GraphQLTypes/index.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
